program PrjOFXReader;

uses
  Forms,
  OFXReader.View in 'OFXReader.View.pas' {Form1},
  OFXReader in 'OFXReader.pas';

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
